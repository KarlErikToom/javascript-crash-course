/**Print out every character from the string:
 * "Frontend Simplified"
 * 
 * @example
 * `F`
 * `r`
 * `o`
 * `n`
 * ...
 * `e`
 * `d`
 */

let str = "Frontend Simplified"


for (let i = 0; i < str.length; i++){
    console.log(str[i])
}
